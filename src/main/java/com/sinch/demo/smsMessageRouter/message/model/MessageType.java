package com.sinch.demo.smsMessageRouter.message.model;

public enum MessageType {   
    SMS
    // Here is an extention point e.g. for MMS
    // and we can similarly create a new MMSMessage class extedning Message
}
